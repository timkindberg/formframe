{
  "name": "@formframe/validation-contract",
  "version": "0.0.0",
  "description": "Validator-agnostic contract suite for FormFrame validation adapters",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=18"
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "src",
    "!**/*.test.ts",
    "!**/*.test.tsx",
    "!**/*.spec.ts",
    "!**/*TestUtils.ts"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean --sourcemap --tsconfig ../../tsconfig.tsup.json",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "json-schema",
    "validation",
    "contract",
    "testing"
  ],
  "author": "Tim Kindberg",
  "license": "MIT",
  "peerDependencies": {
    "@formframe/core": "*",
    "@formframe/input-jsonschema": "*",
    "vitest": "^4.0.0"
  },
  "devDependencies": {
    "@formframe/core": "*",
    "@formframe/input-jsonschema": "*",
    "typescript": "^5.3.3",
    "vitest": "^4.0.9"
  }
}
