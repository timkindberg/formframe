{
  "name": "@jsonschema-form/input-conformance",
  "version": "0.0.0",
  "private": true,
  "description": "The shared input-conformance oracle — expected neutral trees every @jsonschema-form/input-* front-end must produce for equivalent schemas (ADR 033/034). Test-only; imports nothing from any input-* package.",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "require": "./src/index.ts"
    }
  },
  "scripts": {
    "clean": "rm -rf dist"
  },
  "keywords": [
    "conformance",
    "forms"
  ],
  "author": "Tim Kindberg",
  "license": "MIT",
  "peerDependencies": {
    "@jsonschema-form/core": "*",
    "vitest": "^4"
  },
  "devDependencies": {
    "@jsonschema-form/core": "*",
    "typescript": "^5.3.3",
    "vitest": "^4.0.9"
  }
}
