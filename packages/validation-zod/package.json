{
  "name": "@formframe/validation-zod",
  "version": "0.0.0",
  "private": true,
  "description": "Recipe/support helper (ADR 050) — not a FormFrame product package. Recipes and parity tests may depend on this; the library does not ship a validation runtime.",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=18"
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "src",
    "!**/*.test.ts",
    "!**/*.test.tsx",
    "!**/*.spec.ts",
    "!**/*TestUtils.ts"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean --sourcemap --tsconfig ../../tsconfig.tsup.json",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "json-schema",
    "validation",
    "zod"
  ],
  "author": "Tim Kindberg",
  "license": "MIT",
  "peerDependencies": {
    "@formframe/core": "*",
    "zod": "^3 || ^4"
  },
  "devDependencies": {
    "@formframe/core": "*",
    "@formframe/validation-contract": "*",
    "typescript": "^5.3.3",
    "vitest": "^4.0.9",
    "zod": "^4.0.0"
  }
}
